## macOS package bootstrap

This directory contains the macOS package installer used by root `make bootstrap`.

From repo root, prefer:

```bash
make bootstrap
```

If you only want macOS packages/tools:

```bash
make -C _macos_setup
```
